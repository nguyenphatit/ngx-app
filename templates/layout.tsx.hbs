{{#if clientLayout}}
"use client"

{{/if}}
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app"
};

interface {{pascalCase name}}Prop {
  children?: React.ReactNode;
};

export default function {{pascalCase name}}Layout({ children }: {{pascalCase name}}Prop) {
  return (
    <html lang="en">
      <body>
        <main>{children}</main>
      </body>
    </html>
    );
};